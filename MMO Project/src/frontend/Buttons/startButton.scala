/*package frontend.Buttons

import server.API
import backend.Game
import scalafx.Includes._
import scalafx.event.ActionEvent

class startButton(game: Game) extends defaultButton(game) {
  text = "Input username"
  layoutX = 600
  layoutY = 800
  onAction = (event: ActionEvent) => this.ready()

  def ready(): Unit = {
  }
}*/